# Iringbuf的实现

## 思路

阅读nemu代码可知，nemu已经实现了日志功能如下：

```c
static void exec_once(Decode *s, vaddr_t pc) {
  s->pc = pc;
  s->snpc = pc;
  isa_exec_once(s);
  cpu.pc = s->dnpc;
#ifdef CONFIG_ITRACE
  char *p = s->logbuf;
  p += snprintf(p, sizeof(s->logbuf), FMT_WORD ":", s->pc);
  int ilen = s->snpc - s->pc;
  int i;
  uint8_t *inst = (uint8_t *)&s->isa.inst.val;
  for (i = ilen - 1; i >= 0; i --) {
    p += snprintf(p, 4, " %02x", inst[i]);
  }
  int ilen_max = MUXDEF(CONFIG_ISA_x86, 8, 4);
  int space_len = ilen_max - ilen;
  if (space_len < 0) space_len = 0;
  space_len = space_len * 3 + 1;
  memset(p, ' ', space_len);
  p += space_len;

#ifndef CONFIG_ISA_loongarch32r
  void disassemble(char *str, int size, uint64_t pc, uint8_t *code, int nbyte);
  disassemble(p, s->logbuf + sizeof(s->logbuf) - p,
      MUXDEF(CONFIG_ISA_x86, s->snpc, s->pc), (uint8_t *)&s->isa.inst.val, ilen);
#else
  p[0] = '\0'; // the upstream llvm does not support loongarch32r
#endif
#endif
}
```

每一条指令运行的log数据都存储在`s->logbuf中`，剩下的只需要构建一个指定大小的循环队列在每一次执行`exec_once`后将相印的日志信息存储进该循环队列，在程序发生错误时将该循环队列中的所有log信息输出到一个文件中即可。

## 实现

### 循环队列

在本设计中需要的是一个单向队列，在本次设计该队列只有在队列已满时入队会将最早入队的一个节点删除，所以该队列实现如下：

```c
#ifdef CONFIG_ITRACE

#define LOG_LENTH 128
#define LOG_POOL_SIZE 50
typedef struct iringbufnode {
    char log[LOG_LENTH];
    struct iringbufnode *next;
}IRBN;

IRBN *log_pool[LOG_POOL_SIZE] = {};
IRBN *head = NULL, *last = NULL;
size_t ringbufcount = 0;

void new_irbn(char *str) {
	char buf[LOG_LENTH] = "    ";
	char *irbn_str = strcat(buf, str);
	
	IRBN *node = calloc(1, sizeof(IRBN));
	strcpy(node->log, irbn_str);

	if(ringbufcount == 0) {
		node->next = NULL;
		head = node;
		last = node;
		log_pool[0] = node;
	}
	else if (ringbufcount >= LOG_POOL_SIZE) {
		node->next = head->next;
		last->next = node;
		free(log_pool[ringbufcount % LOG_POOL_SIZE]);
		head = node->next;
		last = node;
		log_pool[ringbufcount % LOG_POOL_SIZE] = node;
	}
	else {
		node->next = head;
		last->next = node;
		last = node;
		log_pool[ringbufcount] = node;
	}

	ringbufcount ++;
}

#endif
```

## 疑问

如何判断是那条指令出错了呢？

# mtrace的实现

## 输出格式

```c
// Log("mtrace output begin, memory range %#X - %#X", mtrace_begin, mtrace_end);
log_write("m %#X\t %u\t %s\t %u\n", mtrace_address, mtrace_length, mtrace_operation, mtrace_value);
// Log("mtrace output end");
```

## Kconfig

仿造`itrace`在Kconfig中`config ITRACE`下方添加`config MTRACE`

```
config MTRACE
  depends on TRACE && TARGET_NATIVE_ELF && ENGINE_INTERPRETER
  bool "Enable memorary tracer"
  default n

config MTRACE_COND
  depends on MTRACE
  string "Only trace memory when the condition is true"
  default "true"
```

## 内存读写函数及mtrace_log相关定义

```c
#ifdef CONFIG_MTRACE
static word_t mtrace_begin   = PMEM_LEFT;
static word_t mtrace_end	 = PMEM_RIGHT;
#endif

word_t paddr_read(paddr_t addr, int len) {
  if (likely(in_pmem(addr))) {
    word_t value = pmem_read(addr, len);
    #ifdef CONFIG_MTRACE
    if(mtrace_begin <= addr <= mtrace_end)	MTRACE_LOG(addr, len, "read", value);
    #endif
	return value;
  }
  IFDEF(CONFIG_DEVICE, return mmio_read(addr, len));
  out_of_bound(addr);
  return 0;
}

void paddr_write(paddr_t addr, int len, word_t data) {
  if (likely(in_pmem(addr))) { 
	pmem_write(addr, len, data); 
    #ifdef CONFIG_MTRACE
    if(mtrace_begin <= addr <= mtrace_end)	MTRACE_LOG(addr, len, "write", data);
    #endif
	return; 
  }
  IFDEF(CONFIG_DEVICE, mmio_write(addr, len, data); return);
  out_of_bound(addr);
}
```

关于mtrace_log的实现如下：

```c
#ifdef CONFIG_MTRACE
MTRACE_LOG(mtrace_address, mtrace_length, mtrace_operation, mtrace_value) do {
    log_write("m %#X\t %u\t %s\t %u\n", mtrace_address, mtrace_length, mtrace_operation, mtrace_value);
} while(0)
#endif
```

## 主函数输入参数

暂时不做实现，后续有必要再来实现这一部分功能。

# ftrace的实现

## NEMU传入elf文件

根据`PA2/AM和NEMU的Makefile文件解读.md`可知我们可以修改NEMU下的`monitor.c`文件中的`static int parse_args(int argc, char *argv[])`函数来让NEMU能够读入elf文件，然后通过修改`abstract-machine/scripts/platform/nemu.mk`文件中的`NEMUFLAGS`属性来修改可执行文件读入的参数，也就是主函数输入。

通过上述改动即可完成对elf文件的读入。

具体修改如下：

**monitor.c**



```c
static char *log_file = NULL;
static char *diff_so_file = NULL;
static char *img_file = NULL;
static char *elf_file = NULL;
static int difftest_port = 1234;


static int parse_args(int argc, char *argv[]) {
  const struct option table[] = {
    {"batch"    , no_argument      , NULL, 'b'},
    {"log"      , required_argument, NULL, 'l'},
    {"diff"     , required_argument, NULL, 'd'},
    {"port"     , required_argument, NULL, 'p'},
	{"elf"		, required_argument, NULL, 'e'},
    {"help"     , no_argument      , NULL, 'h'},
    {0          , 0                , NULL,  0 },
  };
  int o;
  while ( (o = getopt_long(argc, argv, "-bhl:d:p:", table, NULL)) != -1) {
    switch (o) {
      case 'b': sdb_set_batch_mode(); break;
      case 'p': sscanf(optarg, "%d", &difftest_port); break;
      case 'l': log_file = optarg; break;
      case 'd': diff_so_file = optarg; break;
	  case 'e':	elf_file = optarg; break;
      case 1: img_file = optarg; return 0;
      default:
        printf("Usage: %s [OPTION...] IMAGE [args]\n\n", argv[0]);
        printf("\t-b,--batch              run with batch mode\n");
        printf("\t-l,--log=FILE           output log to FILE\n");
        printf("\t-d,--diff=REF_SO        run DiffTest with reference REF_SO\n");
        printf("\t-p,--port=PORT          run DiffTest with port PORT\n");
		printf("\t-e,--elf=ELF			  input elf file\n");
        printf("\n");
        exit(0);
    }
  }
  return 0;
}
```

**nemu.mk**

`ftrace`默认关闭

```makefile
FTRACE_MODE?=;
ifeq ($(FTRACE_MODE), true)
	NEMUFLAGS += -e $(IMAGE).elf
endif
```

## Elf

### elf文件结构

#### 结构图

![img](/home/lxt/ysyx-workbench/my_notes/PA2/基础设施(2)实现.assets/elf-file-format.png)

**注意：段（`Segment`）与节（`Section`）的区别。很多地方对两者有所混淆。段是程序执行的必要组成，当多个目标文件链接成一个可执行文件时，会将相同权限的节合并到一个段中。相比而言，节的粒度更小。**

如图所示，为ELF文件的基本结构，其主要由四部分组成：

- ELF Header
- ELF Program Header Table (或称Program Headers、程序头)
- ELF Section Header Table (或称Section Headers、节头表)
- ELF Sections

### elf.h描述

头文件<elf.h>定义了ELF可执行二进制文件的格式。这些文件包括普通可执行文件（normal executable files）、可重定位目标文件（relocatable object files）、核心文件（core file）和共享对象。

使用 ELF 文件格式的可执行文件由 ELF头组成，后面跟着程序头表（program header table）或节头表（section header table），或两者兼有。ELF 头始终位于文件的偏移量零处。程序头表和节头表在文件中的偏移量在 ELF 头中定义。这两个表描述了文件的其余特性。

此头文件将上述头文件描述为 C语言结构体，并且还包括动态部分（dynamic sections）、重定位部分（relocation sections）和符号表（symbol tables）的结构。

#### Basic types

以下类型用于N位架构（N=32，64，`ElfN` 表示`Elf32`或`Elf64`）

- `ElfN_ADDR`：无符号程序地址，`uintN_t`
- `ElfN_off`：无符号文件偏移，`uintN_t`
- `ElfN_Section`：无符号节索引，`uint16_t`
- `ElfN_Versym`：无符号版本标识信息，`uint16_t`
- `Elf_Byte`：`unsigned char`
- `ElfN_Half`：`uint16_t`
- `ElfN_Sword`：`int32_t`
- `ElfN_Word`：`uint32_t`
- `ElfN_Sxword`：`int64_t`
- `ElfN_Xword`：`uint64_t`

#### ELF header (Ehdr)

```c
// The ELF header is described by the type Elf32_Ehdr or Elf64_Ehdr
typedef struct {
    unsigned char e_ident[EI_NIDENT];
    uint16_t      e_type;
    uint16_t      e_machine;
    uint32_t      e_version;
    ElfN_Addr     e_entry;
    ElfN_Off      e_phoff;
    ElfN_Off      e_shoff;
    uint32_t      e_flags;
    uint16_t      e_ehsize;
    uint16_t      e_phentsize;
    uint16_t      e_phnum;
    uint16_t      e_shentsize;
    uint16_t      e_shnum;
    uint16_t      e_shstrndx;
} ElfN_Ehdr;
```

#### Program header (Phdr)

```c
typedef struct {
    uint32_t   p_type;
    Elf32_Off  p_offset;
    Elf32_Addr p_vaddr;
    Elf32_Addr p_paddr;
    uint32_t   p_filesz;
    uint32_t   p_memsz;
    uint32_t   p_flags;
    uint32_t   p_align;
}Elf32_Phdr
```

#### Section header (Shdr)

```c
typedef struct {
    uint32_t   sh_name;
    uint32_t   sh_type;
    uint32_t   sh_flags;
    Elf32_Addr sh_addr;
    Elf32_Off  sh_offset;
    uint32_t   sh_size;
    uint32_t   sh_link;
    uint32_t   sh_info;
    uint32_t   sh_addralign;
    uint32_t   sh_entsize;
} Elf32_Shdr;
```

#### String and symbol tables

```c
typedef struct {
    uint32_t      st_name;
    Elf32_Addr    st_value;
    uint32_t      st_size;
    unsigned char st_info;
    unsigned char st_other;
    uint16_t      st_shndx;
} Elf32_Sym;
```

#### Relocation entries (Rel & Rela)

```c
typedef struct {
    Elf32_Addr r_offset;
    uint32_t   r_info;
} Elf32_Rel;
```

```c
typedef struct {
    Elf32_Sword    d_tag;
    union {
        Elf32_Word d_val;
        Elf32_Addr d_ptr;
    } d_un;
} Elf32_Dyn;
extern Elf32_Dyn _DYNAMIC[];

typedef struct {
    Elf64_Sxword    d_tag;
    union {
        Elf64_Xword d_val;
        Elf64_Addr  d_ptr;
    } d_un;
} Elf64_Dyn;
extern Elf64_Dyn _DYNAMIC[];
```

#### Notes (Nhdr)

```c
typedef struct {
    Elf32_Word n_namesz;
    Elf32_Word n_descsz;
    Elf32_Word n_type;
} Elf32_Nhdr;
```

